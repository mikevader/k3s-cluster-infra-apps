metallb:
  controller:
    logLevel: warn

  speaker:
    logLevel: warn

  prometheus:
    serviceMonitor:
      enabled: true
    namespace: monitoring-stack
    serviceAccount: monitoring-stack-kube-prom-prometheus
