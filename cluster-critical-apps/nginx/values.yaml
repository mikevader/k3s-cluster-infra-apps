nginx:
  controller:
    service:
      annotations:
        metallb.universe.tf/address-pool: external
        metallb.universe.tf/allow-shared-ip: shared-external

    metrics:
      serviceMonitor:
        enable:

    serviceMonitor:
      create: true
